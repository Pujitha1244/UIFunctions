import { createSlice } from '@reduxjs/toolkit';

const data1Slice = createSlice({
  name: 'data1',
  initialState: [],
  reducers: {

  },
});

export const { } = data1Slice.actions;

export default data1Slice.reducer;